import { ANIMATION_TIME_MS, STATE } from "../constants";
import {  animateEdge, animateNode } from "../utils/formatColor";

export const tarjan = async (cy, graph, startNode, isDirected, isWeighted) => {
    console.log('tarjan');
    // Implementation for Tarjan's algorithm
  };