import { ANIMATION_TIME_MS, STATE } from "../constants";
import {  animateEdge, animateNode } from "../utils/formatColor";

export const kosaraju = async (cy, graph, startNode, isDirected, isWeighted) => {
    // console.log('kosaraju');
    // Implementation for Kosaraju's algorithm
  };