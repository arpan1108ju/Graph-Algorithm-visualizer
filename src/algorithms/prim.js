import { ANIMATION_TIME_MS, STATE } from "../constants";
import {  animateEdge, animateNode } from "../utils/formatColor";

export const prim = async (cy, graph, startNode, isDirected, isWeighted) => {
    console.log('prim');
    // Implementation for Prim's algorithm
  };